use crate::Instruction;
pub mod tokens;

/*impl Instruction{
    fn parse(instruction: String) -> Option<Self>{
        if !instruction.contains(','){
            return None;
        }
        
        let parts = instruction.split(',');

    }
}*/